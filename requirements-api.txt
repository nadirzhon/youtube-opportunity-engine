fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2
# Real YouTube provider (only needed when YOUTUBE_API_KEY is set):
google-api-python-client>=2.0
# Postgres store (only needed when DATABASE_URL is a postgres:// URL):
psycopg[binary]>=3.1
