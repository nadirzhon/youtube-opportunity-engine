# Core engine: standard library only — no runtime deps.
# Later phases add: fastapi, uvicorn, sqlalchemy, alembic, pydantic, redis,
# arq, google-api-python-client, numpy, scikit-learn.
[dev]
pytest>=7
