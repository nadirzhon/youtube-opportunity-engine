# Core engine: standard library only.
# API layer (Phase 1):
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2
# Real YouTube provider (Phase 2, only with a key):
google-api-python-client>=2.0

[dev]
pytest>=7
httpx>=0.27
